# Copy this file to .env.local and replace placeholders locally.
# Use the direct Neon endpoint, not a hostname containing -pooler.
SPRING_DATASOURCE_URL=jdbc:postgresql://<DIRECT_NEON_HOST>/<DATABASE>?sslmode=require
SPRING_DATASOURCE_USERNAME=<USERNAME>
SPRING_DATASOURCE_PASSWORD=<PASSWORD>
