# Controlled RAG fixtures

These UTF-8 source documents are version-controlled truth for the local RAG benchmark. The PDF/DOCX ingestion adapters are covered by their parser tests; the benchmark stores the same facts as text fixtures so the evaluation remains reviewable in Git and does not require opaque binary files.
